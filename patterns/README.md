# Pattern folders

- `user/`: LifeGameDxlibVersion3 上で選択範囲から作成したユーザーパターン（RLE）
- `import/`: 外部から取り込んだ RLE パターン
- `metadata.json`: User / Import パターンの表示用メタデータ（お気に入り・カテゴリなど）

`user/*.rle`、`import/*.rle`、`metadata.json` はローカルデータとして扱い、Git には追加しません。
